# Required: your Anthropic API key (set this in Vercel Project Settings → Environment Variables)
ANTHROPIC_API_KEY=

# Optional: override the model. Defaults to claude-opus-4-8.
# If you hit Vercel's 60s function timeout, a faster model helps, e.g.:
# ANTHROPIC_MODEL=claude-sonnet-5
ANTHROPIC_MODEL=
